<?xml version="1.0" encoding="UTF-8"?>
<feed version="1.0" hasPendingRequests="false">
  <company></company>
  <status>200</status>
  <errmsg>OK</errmsg>
  <entry type="datasource">
    <name>KubeVirt_VMI_Discovery</name>
    <displayedas>KubeVirt VMI Discovery</displayedas>
    <description>Discovers running KubeVirt Virtual Machine Instances via Thanos Querier</description>
    <group>KubeVirt</group>
    <tags>kubevirt,openshift,virtualization</tags>
    <technology>Discovers KubeVirt VMIs by querying Thanos/Prometheus metrics</technology>
    <appliesTo>kubevirt.thanos.host &amp;&amp; kubevirt.thanos.token</appliesTo>
    <collectInterval>5</collectInterval>
    <hasMultiInstances>true</hasMultiInstances>
    <useWildValueAsUniqueIdentifier>true</useWildValueAsUniqueIdentifier>
    <collectMethod>script</collectMethod>
    <agentType>0</agentType>
    <enableAutoDiscovery>true</enableAutoDiscovery>
    <autoDiscoveryConfig>
      <persistentInstance>true</persistentInstance>
      <deleteInactiveInstance>true</deleteInactiveInstance>
      <disableInstance>false</disableInstance>
      <scheduleInterval>15</scheduleInterval>
      <method>ad_script</method>
      <type>embeded</type>
      <groovyScript><![CDATA[import groovy.json.JsonSlurper

class ThanosClient {
    private String host, token, baseUrl
    private int port
    private boolean ssl

    ThanosClient(Map p) {
        this.host = p.get("kubevirt.thanos.host")
        this.token = p.get("kubevirt.thanos.token")
        this.port = (p.get("kubevirt.thanos.port") ?: "443") as int
        this.ssl = (p.get("kubevirt.thanos.ssl") ?: "true").toLowerCase() == "true"
        if (!host || !token) throw new IllegalArgumentException("Missing config")
        def proto = ssl ? "https" : "http"
        def ps = ((ssl && port == 443) || (!ssl && port == 80)) ? "" : ":${port}"
        this.baseUrl = "${proto}://${host}${ps}"
    }

    List queryWithLabels(String promql) {
        def url = "${baseUrl}/api/v1/query?query=${java.net.URLEncoder.encode(promql, 'UTF-8')}"
        try {
            def resp = executeHttpGet(url)
            def json = new JsonSlurper().parseText(resp)
            if (json.status != "success" || !json.data?.result) return []
            return json.data.result.collect { r -> [r.metric ?: [:], r.value ? r.value[1] : null] }
        } catch (Exception e) { return [] }
    }

    private String executeHttpGet(String url) {
        try {
            def http = Class.forName("com.santaba.agent.groovyapi.http.HTTP").newInstance()
            http.open(url); http.setHeader("Authorization", "Bearer ${token}")
            def r = http.get(); http.close(); return r
        } catch (ClassNotFoundException e) {
            def c = new URL(url).openConnection() as HttpURLConnection
            c.setRequestMethod("GET"); c.setRequestProperty("Authorization", "Bearer ${token}")
            c.setConnectTimeout(30000); c.setReadTimeout(60000)
            if (ssl) { try {
                def sc = javax.net.ssl.SSLContext.getInstance("TLS")
                sc.init(null, [new javax.net.ssl.X509TrustManager() {
                    void checkClientTrusted(java.security.cert.X509Certificate[] x, String a) {}
                    void checkServerTrusted(java.security.cert.X509Certificate[] x, String a) {}
                    java.security.cert.X509Certificate[] getAcceptedIssuers() { null }
                }] as javax.net.ssl.TrustManager[], new java.security.SecureRandom())
                c.setSSLSocketFactory(sc.getSocketFactory()); c.setHostnameVerifier({ h, s -> true })
            } catch (Exception ex) {} }
            if (c.getResponseCode() != 200) throw new RuntimeException("HTTP ${c.getResponseCode()}")
            return c.getInputStream().text
        }
    }
}

try {
    def client = new ThanosClient(hostProps)
    def discovered = [] as Set
    client.queryWithLabels('kubevirt_vmi_info{phase="running"}').each { labels, value ->
        def ns = labels.get("namespace")
        def name = labels.get("name")
        if (!ns || !name) return
        def key = "${ns}/${name}"
        if (discovered.contains(key)) return
        discovered.add(key)
        def node = labels.get("node") ?: "unknown"
        def guestOs = labels.get("guest_os_name") ?: ""
        def props = ["auto.kubevirt.namespace=${ns}", "auto.kubevirt.vmi_name=${name}", "auto.kubevirt.node=${node}"]
        if (guestOs) props << "auto.kubevirt.guest_os=${guestOs}"
        println "${key}##${name}##VM ${name} in ${ns}" + (guestOs ? " (${guestOs})" : "") + "####${props.join('&')}"
    }
    return 0
} catch (Exception e) {
    System.err.println "ERROR: ${e.message}"
    return 1
}]]></groovyScript>
    </autoDiscoveryConfig>
    <collectorAttribute>
      <name>script</name>
      <scriptType>embed</scriptType>
      <groovyScript><![CDATA[import groovy.json.JsonSlurper

class ThanosClient {
    private String host, token, baseUrl
    private int port
    private boolean ssl

    ThanosClient(Map p) {
        this.host = p.get("kubevirt.thanos.host")
        this.token = p.get("kubevirt.thanos.token")
        this.port = (p.get("kubevirt.thanos.port") ?: "443") as int
        this.ssl = (p.get("kubevirt.thanos.ssl") ?: "true").toLowerCase() == "true"
        if (!host || !token) throw new IllegalArgumentException("Missing config")
        def proto = ssl ? "https" : "http"
        def ps = ((ssl && port == 443) || (!ssl && port == 80)) ? "" : ":${port}"
        this.baseUrl = "${proto}://${host}${ps}"
    }

    Map queryGroupedByKey(String promql, List<String> keys, String sep = "/") {
        def results = [:]
        def url = "${baseUrl}/api/v1/query?query=${java.net.URLEncoder.encode(promql, 'UTF-8')}"
        try {
            def resp = executeHttpGet(url)
            def json = new JsonSlurper().parseText(resp)
            if (json.status != "success" || !json.data?.result) return results
            json.data.result.each { r ->
                def labels = r.metric ?: [:]
                def keyParts = keys.collect { labels[it] ?: "" }
                if (keyParts.every { it }) results[keyParts.join(sep)] = r.value ? r.value[1] : null
            }
        } catch (Exception e) {}
        return results
    }

    private String executeHttpGet(String url) {
        try {
            def http = Class.forName("com.santaba.agent.groovyapi.http.HTTP").newInstance()
            http.open(url); http.setHeader("Authorization", "Bearer ${token}")
            def r = http.get(); http.close(); return r
        } catch (ClassNotFoundException e) {
            def c = new URL(url).openConnection() as HttpURLConnection
            c.setRequestMethod("GET"); c.setRequestProperty("Authorization", "Bearer ${token}")
            c.setConnectTimeout(30000); c.setReadTimeout(60000)
            if (ssl) { try {
                def sc = javax.net.ssl.SSLContext.getInstance("TLS")
                sc.init(null, [new javax.net.ssl.X509TrustManager() {
                    void checkClientTrusted(java.security.cert.X509Certificate[] x, String a) {}
                    void checkServerTrusted(java.security.cert.X509Certificate[] x, String a) {}
                    java.security.cert.X509Certificate[] getAcceptedIssuers() { null }
                }] as javax.net.ssl.TrustManager[], new java.security.SecureRandom())
                c.setSSLSocketFactory(sc.getSocketFactory()); c.setHostnameVerifier({ h, s -> true })
            } catch (Exception ex) {} }
            if (c.getResponseCode() != 200) throw new RuntimeException("HTTP ${c.getResponseCode()}")
            return c.getInputStream().text
        }
    }
}

try {
    def client = new ThanosClient(hostProps)
    def wildvalue = instanceProps.get("wildvalue")
    if (!wildvalue) { println "Status=0"; return 0 }
    def running = client.queryGroupedByKey('kubevirt_vmi_info{phase="running"}', ["namespace", "name"])
    println "Status=${running.containsKey(wildvalue) ? 1 : 0}"
    return 0
} catch (Exception e) {
    System.err.println "ERROR: ${e.message}"
    return 1
}]]></groovyScript>
    </collectorAttribute>
    <datapoints>
      <datapoint>
        <name>Status</name>
        <description>VMI running status (1=running, 0=not running)</description>
        <dataType>7</dataType>
        <type>2</type>
        <postprocessormethod>namevalue</postprocessormethod>
        <postprocessorparam>Status</postprocessorparam>
        <alertexpr>&lt; 1</alertexpr>
        <alertTransitionInterval>3</alertTransitionInterval>
        <alertClearTransitionInterval>1</alertClearTransitionInterval>
      </datapoint>
    </datapoints>
    <graphs>
      <graph>
        <name>VMI Status</name>
        <title>VMI Running Status</title>
        <verticallabel>Status</verticallabel>
        <datapoints>
          <datapoint>
            <name>Status</name>
            <aggregation>min</aggregation>
          </datapoint>
        </datapoints>
      </graph>
    </graphs>
  </entry>
</feed>
