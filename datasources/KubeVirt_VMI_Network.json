{
  "name": "KubeVirt_VMI_Network",
  "displayName": "KubeVirt VMI Network",
  "description": "Network metrics per interface for KubeVirt Virtual Machine Instances",
  "group": "KubeVirt",
  "tags": "kubevirt,openshift,virtualization,network",
  "appliesTo": "kubevirt.thanos.host && kubevirt.thanos.token",
  "technology": "Collects per-interface network metrics from Thanos/Prometheus for KubeVirt VMIs",
  "collectMethod": "batchscript",
  "collectInterval": 60,
  "multiInstance": true,
  "hasMultiInstances": true,
  "useWildValueAsUniqueIdentifier": true,
  "activeDiscovery": {
    "method": "ad_script",
    "schedule": 900,
    "deleteInactiveInstances": true,
    "disableInstance": false,
    "script": {
      "type": "embeddedGroovyScript",
      "content": "// v4 - Network Interface Discovery\nimport groovy.json.JsonSlurper\n\nclass ThanosClient {\n    private String host, token, baseUrl\n    private int port\n    private boolean ssl\n\n    ThanosClient(Map p) {\n        this.host = p.get(\"kubevirt.thanos.host\")\n        this.token = p.get(\"kubevirt.thanos.token\")\n        this.port = (p.get(\"kubevirt.thanos.port\") ?: \"443\") as int\n        this.ssl = (p.get(\"kubevirt.thanos.ssl\") ?: \"true\").toLowerCase() == \"true\"\n        if (!host || !token) throw new IllegalArgumentException(\"Missing config\")\n        def proto = ssl ? \"https\" : \"http\"\n        def ps = ((ssl && port == 443) || (!ssl && port == 80)) ? \"\" : \":${port}\"\n        this.baseUrl = \"${proto}://${host}${ps}\"\n    }\n\n    List queryWithLabels(String promql) {\n        def url = \"${baseUrl}/api/v1/query?query=${java.net.URLEncoder.encode(promql, 'UTF-8')}\"\n        try {\n            def resp = executeHttpGet(url)\n            def json = new JsonSlurper().parseText(resp)\n            if (json.status != \"success\" || !json.data?.result) return []\n            return json.data.result.collect { r -> [r.metric ?: [:], r.value ? r.value[1] : null] }\n        } catch (Exception e) { return [] }\n    }\n\n    private String executeHttpGet(String url) {\n        try {\n            def http = Class.forName(\"com.santaba.agent.groovyapi.http.HTTP\").newInstance()\n            http.open(url); http.setHeader(\"Authorization\", \"Bearer ${token}\")\n            def r = http.get(); http.close(); return r\n        } catch (ClassNotFoundException e) {\n            def c = new URL(url).openConnection() as HttpURLConnection\n            c.setRequestMethod(\"GET\"); c.setRequestProperty(\"Authorization\", \"Bearer ${token}\")\n            c.setConnectTimeout(30000); c.setReadTimeout(60000)\n            if (ssl) { try {\n                def sc = javax.net.ssl.SSLContext.getInstance(\"TLS\")\n                sc.init(null, [new javax.net.ssl.X509TrustManager() {\n                    void checkClientTrusted(java.security.cert.X509Certificate[] x, String a) {}\n                    void checkServerTrusted(java.security.cert.X509Certificate[] x, String a) {}\n                    java.security.cert.X509Certificate[] getAcceptedIssuers() { null }\n                }] as javax.net.ssl.TrustManager[], new java.security.SecureRandom())\n                c.setSSLSocketFactory(sc.getSocketFactory()); c.setHostnameVerifier({ h, s -> true })\n            } catch (Exception ex) {} }\n            if (c.getResponseCode() != 200) throw new RuntimeException(\"HTTP ${c.getResponseCode()}\")\n            return c.getInputStream().text\n        }\n    }\n}\n\ntry {\n    def client = new ThanosClient(hostProps)\n    def discovered = [] as Set\n    client.queryWithLabels('kubevirt_vmi_network_receive_bytes_total').each { labels, value ->\n        def ns = labels.get(\"namespace\")\n        def name = labels.get(\"name\")\n        def iface = labels.get(\"interface\")\n        if (!ns || !name || !iface) return\n        def key = \"${ns}/${name}/${iface}\"\n        if (discovered.contains(key)) return\n        discovered.add(key)\n        def props = [\"auto.kubevirt.namespace=${ns}\", \"auto.kubevirt.vmi_name=${name}\", \"auto.kubevirt.interface=${iface}\"]\n        println \"${key}##${name}/${iface}##Interface ${iface} on VM ${name}####${props.join('&')}\"\n    }\n    return 0\n} catch (Exception e) {\n    System.err.println \"ERROR: ${e.message}\"\n    return 1\n}"
    }
  },
  "dataPoints": [
    {
      "name": "rx_bytes",
      "description": "Receive throughput (bytes/sec)",
      "type": "gauge"
    },
    {
      "name": "tx_bytes",
      "description": "Transmit throughput (bytes/sec)",
      "type": "gauge"
    },
    {
      "name": "rx_packets",
      "description": "Receive packet rate (packets/sec)",
      "type": "gauge"
    },
    {
      "name": "tx_packets",
      "description": "Transmit packet rate (packets/sec)",
      "type": "gauge"
    },
    {
      "name": "rx_errors",
      "description": "Receive errors per second",
      "type": "gauge",
      "alertExpr": "> 0",
      "alertExprNote": "Alert on any receive errors"
    },
    {
      "name": "tx_errors",
      "description": "Transmit errors per second",
      "type": "gauge",
      "alertExpr": "> 0",
      "alertExprNote": "Alert on any transmit errors"
    },
    {
      "name": "rx_dropped",
      "description": "Receive dropped packets per second",
      "type": "gauge",
      "alertExpr": "> 10",
      "alertExprNote": "Alert when drops exceed 10/sec"
    },
    {
      "name": "tx_dropped",
      "description": "Transmit dropped packets per second",
      "type": "gauge",
      "alertExpr": "> 10",
      "alertExprNote": "Alert when drops exceed 10/sec"
    }
  ],
  "collectScript": {
    "type": "embeddedGroovyScript",
    "content": "// v4 - Network Collection BATCHSCRIPT\nimport groovy.json.JsonSlurper\n\nclass ThanosClient {\n    private String host, token, baseUrl\n    private int port\n    private boolean ssl\n\n    ThanosClient(Map p) {\n        this.host = p.get(\"kubevirt.thanos.host\")\n        this.token = p.get(\"kubevirt.thanos.token\")\n        this.port = (p.get(\"kubevirt.thanos.port\") ?: \"443\") as int\n        this.ssl = (p.get(\"kubevirt.thanos.ssl\") ?: \"true\").toLowerCase() == \"true\"\n        if (!host || !token) throw new IllegalArgumentException(\"Missing config\")\n        def proto = ssl ? \"https\" : \"http\"\n        def ps = ((ssl && port == 443) || (!ssl && port == 80)) ? \"\" : \":${port}\"\n        this.baseUrl = \"${proto}://${host}${ps}\"\n    }\n\n    Map queryGroupedByKey(String promql, List<String> keys, String sep = \"/\") {\n        def results = [:]\n        def url = \"${baseUrl}/api/v1/query?query=${java.net.URLEncoder.encode(promql, 'UTF-8')}\"\n        try {\n            def resp = executeHttpGet(url)\n            def json = new JsonSlurper().parseText(resp)\n            if (json.status != \"success\" || !json.data?.result) return results\n            json.data.result.each { r ->\n                def labels = r.metric ?: [:]\n                def keyParts = keys.collect { labels[it] ?: \"\" }\n                if (keyParts.every { it }) results[keyParts.join(sep)] = r.value ? r.value[1] : null\n            }\n        } catch (Exception e) {}\n        return results\n    }\n\n    private String executeHttpGet(String url) {\n        try {\n            def http = Class.forName(\"com.santaba.agent.groovyapi.http.HTTP\").newInstance()\n            http.open(url); http.setHeader(\"Authorization\", \"Bearer ${token}\")\n            def r = http.get(); http.close(); return r\n        } catch (ClassNotFoundException e) {\n            def c = new URL(url).openConnection() as HttpURLConnection\n            c.setRequestMethod(\"GET\"); c.setRequestProperty(\"Authorization\", \"Bearer ${token}\")\n            c.setConnectTimeout(30000); c.setReadTimeout(60000)\n            if (ssl) { try {\n                def sc = javax.net.ssl.SSLContext.getInstance(\"TLS\")\n                sc.init(null, [new javax.net.ssl.X509TrustManager() {\n                    void checkClientTrusted(java.security.cert.X509Certificate[] x, String a) {}\n                    void checkServerTrusted(java.security.cert.X509Certificate[] x, String a) {}\n                    java.security.cert.X509Certificate[] getAcceptedIssuers() { null }\n                }] as javax.net.ssl.TrustManager[], new java.security.SecureRandom())\n                c.setSSLSocketFactory(sc.getSocketFactory()); c.setHostnameVerifier({ h, s -> true })\n            } catch (Exception ex) {} }\n            if (c.getResponseCode() != 200) throw new RuntimeException(\"HTTP ${c.getResponseCode()}\")\n            return c.getInputStream().text\n        }\n    }\n}\n\ntry {\n    def client = new ThanosClient(hostProps)\n    def metrics = [\n        \"rx_bytes\": 'rate(kubevirt_vmi_network_receive_bytes_total[5m])',\n        \"tx_bytes\": 'rate(kubevirt_vmi_network_transmit_bytes_total[5m])',\n        \"rx_packets\": 'rate(kubevirt_vmi_network_receive_packets_total[5m])',\n        \"tx_packets\": 'rate(kubevirt_vmi_network_transmit_packets_total[5m])',\n        \"rx_errors\": 'rate(kubevirt_vmi_network_receive_errors_total[5m])',\n        \"tx_errors\": 'rate(kubevirt_vmi_network_transmit_errors_total[5m])',\n        \"rx_dropped\": 'rate(kubevirt_vmi_network_receive_packets_dropped_total[5m])',\n        \"tx_dropped\": 'rate(kubevirt_vmi_network_transmit_packets_dropped_total[5m])'\n    ]\n    def allData = [:]\n    metrics.each { dp, q -> client.queryGroupedByKey(q, [\"namespace\", \"name\", \"interface\"]).each { k, v -> if (!allData[k]) allData[k] = [:]; allData[k][dp] = v } }\n    allData.each { wv, dps -> dps.each { dp, val -> if (val != null) println \"${wv}.${dp}=${val}\" } }\n    return 0\n} catch (Exception e) {\n    System.err.println \"ERROR: ${e.message}\"\n    return 1\n}"
  },
  "graphs": [
    {
      "name": "Network Throughput",
      "title": "Network Throughput (bytes/sec)",
      "dataPoints": [
        {"name": "rx_bytes", "aggregation": "avg"},
        {"name": "tx_bytes", "aggregation": "avg"}
      ]
    },
    {
      "name": "Packet Rate",
      "title": "Packet Rate (packets/sec)",
      "dataPoints": [
        {"name": "rx_packets", "aggregation": "avg"},
        {"name": "tx_packets", "aggregation": "avg"}
      ]
    },
    {
      "name": "Errors and Drops",
      "title": "Network Errors and Dropped Packets",
      "dataPoints": [
        {"name": "rx_errors", "aggregation": "avg"},
        {"name": "tx_errors", "aggregation": "avg"},
        {"name": "rx_dropped", "aggregation": "avg"},
        {"name": "tx_dropped", "aggregation": "avg"}
      ]
    }
  ]
}
