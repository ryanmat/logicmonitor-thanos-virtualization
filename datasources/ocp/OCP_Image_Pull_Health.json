{
  "collectionMethod": "script",
  "datapoints": [
    {
      "max": "",
      "triggerInterval": 5,
      "dataType": 7,
      "maxDigits": 4,
      "description": "Image pulls succeeded per second (per-node)",
      "interpretMethod": "namevalue",
      "interpretExpr": "pull_success_rate",
      "type": "gauge",
      "clearInterval": 0,
      "originId": "",
      "useValue": "output",
      "min": "0",
      "name": "pull_success_rate",
      "noData": "Do not trigger an alert",
      "statusDisplayNames": []
    },
    {
      "max": "",
      "triggerInterval": 5,
      "dataType": 7,
      "maxDigits": 4,
      "description": "Image pulls failed per second (per-node)",
      "interpretMethod": "namevalue",
      "threshold": "> 0",
      "interpretExpr": "pull_failure_rate",
      "type": "gauge",
      "clearInterval": 0,
      "originId": "",
      "useValue": "output",
      "min": "0",
      "name": "pull_failure_rate",
      "noData": "Do not trigger an alert",
      "statusDisplayNames": []
    },
    {
      "max": "",
      "triggerInterval": 5,
      "dataType": 7,
      "maxDigits": 4,
      "description": "Image pull duration p99 in milliseconds (per-node)",
      "interpretMethod": "namevalue",
      "threshold": "> 30000",
      "interpretExpr": "pull_duration_p99_ms",
      "type": "gauge",
      "clearInterval": 0,
      "originId": "",
      "useValue": "output",
      "min": "0",
      "name": "pull_duration_p99_ms",
      "noData": "Do not trigger an alert",
      "statusDisplayNames": []
    },
    {
      "max": "",
      "triggerInterval": 5,
      "dataType": 7,
      "maxDigits": 4,
      "description": "Total image pull failures per second cluster-wide",
      "interpretMethod": "namevalue",
      "threshold": "> 0",
      "interpretExpr": "cluster_pull_failures",
      "type": "gauge",
      "clearInterval": 0,
      "originId": "",
      "useValue": "output",
      "min": "0",
      "name": "cluster_pull_failures",
      "noData": "Do not trigger an alert",
      "statusDisplayNames": []
    },
    {
      "max": "",
      "triggerInterval": 5,
      "dataType": 7,
      "maxDigits": 4,
      "description": "Pods in ImagePullBackOff state cluster-wide",
      "interpretMethod": "namevalue",
      "threshold": "> 0",
      "interpretExpr": "cluster_imagepullbackoff",
      "type": "gauge",
      "clearInterval": 0,
      "originId": "",
      "useValue": "output",
      "min": "0",
      "name": "cluster_imagepullbackoff",
      "noData": "Do not trigger an alert",
      "statusDisplayNames": []
    }
  ],
  "searchKeywords": "openshift,ocp,image,pull,crio,failure,imagepullbackoff,node",
  "displayedAs": "OCP Image Pull Health",
  "description": "Per-node CRI-O image pull rates and cluster-wide pull failure counts via Thanos Querier",
  "appliesTo": "openshift.thanos.host && openshift.thanos.pass",
  "activeDiscovery": {
    "groupExpr": "",
    "deleteInactiveInstances": true,
    "groupMethod": "ilp",
    "discoveryMethod": "ad_script",
    "autoDeleteInstances": false,
    "disableDiscoveredInstances": false,
    "filters": [],
    "params": {
      "type": "groovy",
      "content": "// Description: Discovers OCP nodes via CRI-O image pull metrics from Thanos Querier.\n// Description: Returns one instance per node plus a static cluster aggregate instance.\nimport groovy.json.JsonSlurper\nimport javax.net.ssl.*\nimport java.security.cert.X509Certificate\n\ndef host = hostProps.get(\"openshift.thanos.host\")\ndef token = hostProps.get(\"openshift.thanos.pass\")\ndef port = hostProps.get(\"openshift.thanos.port\") ?: \"443\"\ndef useSsl = (hostProps.get(\"openshift.thanos.ssl\") ?: \"true\").toLowerCase() == \"true\"\n\nif (!host || !token) {\n    println \"openshift.thanos.host and openshift.thanos.pass properties are required\"\n    return 1\n}\n\ndef trustAllCerts = [\n    checkClientTrusted: { chain, authType -> },\n    checkServerTrusted: { chain, authType -> },\n    getAcceptedIssuers: { null }\n] as X509TrustManager\n\ndef sc = SSLContext.getInstance(\"TLS\")\nsc.init(null, [trustAllCerts] as TrustManager[], null)\nHttpsURLConnection.setDefaultSSLSocketFactory(sc.getSocketFactory())\nHttpsURLConnection.setDefaultHostnameVerifier({ hostname, session -> true } as HostnameVerifier)\n\ndef proto = useSsl ? \"https\" : \"http\"\ndef baseUrl = \"${proto}://${host}:${port}\"\n\ntry {\n    def query = 'container_runtime_crio_image_pulls_success_total'\n    def encoded = java.net.URLEncoder.encode(query, \"UTF-8\")\n    def url = new URL(\"${baseUrl}/api/v1/query?query=${encoded}\")\n    def conn = url.openConnection()\n    conn.setRequestProperty(\"Authorization\", \"Bearer ${token}\")\n    conn.setConnectTimeout(30000)\n    conn.setReadTimeout(30000)\n\n    if (conn.responseCode == 200) {\n        def response = conn.inputStream.text\n        def json = new JsonSlurper().parseText(response)\n\n        if (json.status == \"success\" && json.data?.result) {\n            def seen = [] as Set\n            json.data.result.each { result ->\n                def labels = result.metric\n                def instance = labels.instance ?: \"unknown\"\n                def node = labels.node ?: instance\n\n                if (!seen.contains(instance)) {\n                    seen.add(instance)\n                    def displayName = node\n                    def description = \"CRI-O image pull metrics for ${node}\"\n\n                    println \"${instance}##${displayName}##${description}####auto.node.instance=${instance}&auto.node.name=${node}\"\n                }\n            }\n            // Static cluster aggregate instance\n            println \"cluster##Cluster Aggregate##Cluster-wide image pull health summary\"\n        }\n    } else {\n        System.err.println(\"Discovery failed: HTTP ${conn.responseCode}\")\n    }\n} catch (Exception e) {\n    System.err.println(\"Discovery error: ${e.message}\")\n    return 1\n}\nreturn 0\n"
    },
    "discoveryInterval": "15m",
    "enabled": true,
    "showDeletedInstanceDays": 0
  },
  "collectionAttrs": {
    "type": "groovy",
    "content": "// Description: Collects CRI-O image pull metrics per node and cluster aggregates via Thanos Querier.\n// Description: Branches on wildvalue to run per-node or cluster-wide queries.\nimport groovy.json.JsonSlurper\nimport javax.net.ssl.*\nimport java.security.cert.X509Certificate\n\ndef host = hostProps.get(\"openshift.thanos.host\")\ndef token = hostProps.get(\"openshift.thanos.pass\")\ndef port = hostProps.get(\"openshift.thanos.port\") ?: \"443\"\ndef useSsl = (hostProps.get(\"openshift.thanos.ssl\") ?: \"true\").toLowerCase() == \"true\"\n\ndef wildvalue = instanceProps.get(\"wildvalue\")\ndef nodeInstance = instanceProps.get(\"auto.node.instance\") ?: wildvalue\n\ndef results = [\n    \"pull_success_rate\": 0,\n    \"pull_failure_rate\": 0,\n    \"pull_duration_p99_ms\": 0,\n    \"cluster_pull_failures\": 0,\n    \"cluster_imagepullbackoff\": 0\n]\n\nif (!host || !token) {\n    results.each { k, v -> println(\"${k}:${v}\") }\n    return 0\n}\n\ndef trustAllCerts = [\n    checkClientTrusted: { chain, authType -> },\n    checkServerTrusted: { chain, authType -> },\n    getAcceptedIssuers: { null }\n] as X509TrustManager\n\ndef sc = SSLContext.getInstance(\"TLS\")\nsc.init(null, [trustAllCerts] as TrustManager[], null)\nHttpsURLConnection.setDefaultSSLSocketFactory(sc.getSocketFactory())\nHttpsURLConnection.setDefaultHostnameVerifier({ hostname, session -> true } as HostnameVerifier)\n\ndef proto = useSsl ? \"https\" : \"http\"\ndef baseUrl = \"${proto}://${host}:${port}\"\n\ndef executeQuery(String promql, String baseUrl, String token) {\n    try {\n        def encoded = java.net.URLEncoder.encode(promql, \"UTF-8\")\n        def url = new URL(\"${baseUrl}/api/v1/query?query=${encoded}\")\n        def conn = url.openConnection()\n        conn.setRequestProperty(\"Authorization\", \"Bearer ${token}\")\n        conn.setConnectTimeout(30000)\n        conn.setReadTimeout(30000)\n\n        if (conn.responseCode == 200) {\n            def response = conn.inputStream.text\n            def json = new JsonSlurper().parseText(response)\n            if (json.status == \"success\" && json.data?.result?.size() > 0) {\n                def val = json.data.result[0].value[1]\n                return val?.toString()?.isNumber() ? val : null\n            }\n        } else {\n            System.err.println(\"Query failed: HTTP ${conn.responseCode} for: ${promql}\")\n        }\n    } catch (Exception e) {\n        System.err.println(\"Query error for ${promql}: ${e.message}\")\n    }\n    return null\n}\n\ndef metrics = [:]\ndef msMultipliers = [] as Set\n\nif (wildvalue == \"cluster\") {\n    metrics = [\n        \"cluster_pull_failures\": 'sum(rate(container_runtime_crio_image_pulls_failure_total[5m])) or vector(0)',\n        \"cluster_imagepullbackoff\": 'count(kube_pod_container_status_waiting_reason{reason=\"ImagePullBackOff\"} == 1) or vector(0)'\n    ]\n} else {\n    def nodeIp = nodeInstance.split(\":\")[0]\n    metrics = [\n        \"pull_success_rate\": 'rate(container_runtime_crio_image_pulls_success_total{instance=\"' + nodeInstance + '\"}[5m])',\n        \"pull_failure_rate\": 'rate(container_runtime_crio_image_pulls_failure_total{instance=\"' + nodeInstance + '\"}[5m])',\n        \"pull_duration_p99_ms\": 'histogram_quantile(0.99, rate(kubelet_runtime_operations_duration_seconds_bucket{operation_type=\"image_pull\",instance=~\"' + nodeIp + '.*\"}[5m]))'\n    ]\n    msMultipliers = [\"pull_duration_p99_ms\"] as Set\n}\n\nmetrics.each { dp, query ->\n    def val = executeQuery(query, baseUrl, token)\n    if (val != null) {\n        def dval = val as Double\n        results[dp] = msMultipliers.contains(dp) ? dval * 1000 : dval\n    }\n}\n\nresults.each { k, v -> println(\"${k}:${v}\") }\nreturn 0"
  },
  "type": 0,
  "technicalNotes": "N+1 instances: per-node CRI-O metrics plus cluster aggregate. Collection branches on wildvalue. kubelet histogram_quantile p99 converted to ms.",
  "useWildValueAsUniqueIdentifier": false,
  "version": 1774700000,
  "registryMetadata": {
    "currentUuid": "",
    "originalUuid": "",
    "locatorId": "",
    "authorCompanyUuid": "56c5c6a6-58e4-4b5c-9d7e-629ce3fe75cf",
    "registryVersion": "1.0.0",
    "checksum": "",
    "namespace": "",
    "registryId": "",
    "isChangedFromOrigin": false,
    "status": ""
  },
  "multiInstance": true,
  "name": "OCP_Image_Pull_Health",
  "collectionInterval": "1m",
  "dataSourceType": 1,
  "group": "OCP"
}